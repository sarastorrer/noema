import {runtime,database} from './db';
export type Contact={id:string;name:string;email:string;subject:string;message:string;delivery:string;created_at:string};
export function mailReady(){const e=runtime();return !!(e.CONTACT_EMAIL&&e.RESEND_API_KEY&&e.CONTACT_FROM)}
export async function deliver(contact:Contact){const e=runtime();if(!mailReady())return false;try{const response=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${e.RESEND_API_KEY}`,'Content-Type':'application/json','Idempotency-Key':`noema-${contact.id}`},body:JSON.stringify({from:e.CONTACT_FROM,to:[e.CONTACT_EMAIL],reply_to:contact.email,subject:`[NOEMA] ${contact.subject.replace(/[\r\n]/g,' ')}`,text:`Nome: ${contact.name}\nE-mail: ${contact.email}\n\n${contact.message}`}),signal:AbortSignal.timeout(10000)});if(!response.ok)return false;await database().prepare("UPDATE contacts SET delivery='forwarded' WHERE id=?").bind(contact.id).run();return true}catch{return false}}
